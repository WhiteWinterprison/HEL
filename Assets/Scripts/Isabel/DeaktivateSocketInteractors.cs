using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeaktivateSocketInteractors : MonoBehaviour
{
    //// Start is called before the first frame update
    //void Start()
    //{
    //    if (PhotonNetwork.InRoom)
    //    {
    //        foreach (GameObject obj in GameObject.FindGameObjectsWithTag("VRSocket")) //find all players
    //        {
    //            if (!obj.GetComponent<PhotonView>().IsMine) //find the ones that are not this player
    //            {
    //                //and disable their cameras
    //                foreach (Camera cam in obj.GetComponentsInChildren<Camera>())
    //                {
    //                    cam.enabled = false;
    //                }
    //            }
    //        }
    //    }
    //}

}
